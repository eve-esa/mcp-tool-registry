# Fire Detection MCP Server — Dependencies
mcp[cli]>=1.2.0
httpx>=0.27.0
pyshp>=2.3.0     # For reading local EFFIS shapefile cache
python-dotenv>=1.0.0
matplotlib>=3.8.0  # For compute_metrics time-series plots
numpy>=1.24.0      # For local stats computation from ORBIT TIFFs
tifffile>=2023.7.0 # Lightweight multi-band TIFF reader (depends on numpy)
